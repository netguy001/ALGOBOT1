version: "3.9"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5000
      - FLASK_DEBUG=false
      - ML_ENABLED=false
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
